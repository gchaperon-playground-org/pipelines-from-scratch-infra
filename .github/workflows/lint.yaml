name: lint
on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master
jobs:
  terraform-fmt:
    runs-on: ubuntu-latest
    steps:
      - uses: hashicorp/setup-terraform@v3
      - run: terraform fmt -check -recursive
